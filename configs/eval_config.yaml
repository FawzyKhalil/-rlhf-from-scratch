# Evaluation configuration — Phase 3

models:
  ppo: checkpoints/ppo/best_ppo
  sft: checkpoints/sft/best_sft
  reward_model: checkpoints/rm/best_rm.pt

evaluation:
  n_prompts: 500             # prompts from the held-out test split
  max_new_tokens: 128
  temperature: 1.0
  seed: 42

outputs:
  figures_dir: results/figures
  tables_dir: results/tables
